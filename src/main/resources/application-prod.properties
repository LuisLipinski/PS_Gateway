app.services.login-url=${PS_LOGIN_URL}
app.services.orchestrator-url=${PS_ORCHESTRATOR_URL}
app.services.user-url=${PS_USER_URL}
app.services.contrato-url=${PS_CONTRATO_URL}
app.internal-key=${INTERNAL_API_KEY}
app.jwt.secret-key=${JWT_SECRET_KEY}
app.jwt.issuer=${JWT_ISSUER:ps-login}
app.cors.allowed-origins=${ALLOWED_ORIGINS:}
app.git-commit=${GIT_COMMIT:unknown}
